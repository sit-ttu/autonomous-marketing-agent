---
summary: "CLI reference for `foxfang clawbot` (legacy alias namespace)"
read_when:
  - You maintain older scripts using `foxfang clawbot ...`
  - You need migration guidance to current commands
title: "clawbot"
---

# `foxfang clawbot`

Legacy alias namespace kept for backwards compatibility.

Current supported alias:

- `foxfang clawbot qr` (same behavior as [`foxfang qr`](/cli/qr))

## Migration

Prefer modern top-level commands directly:

- `foxfang clawbot qr` -> `foxfang qr`
